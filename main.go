package main

func main() {
	/*	var a *string
		fmt.Println("LMSLMS %+v", a)

		menuItems := &MenuItem{
			Children: []*MenuItem{
				&MenuItem{
					Children: []*MenuItem{
						&MenuItem{
							Handles: []string{"7", "8", "9"},
						},
					},
					Handles: []string{"4", "5", "6"},
				},
			},
			Handles: []string{"1", "2", "3"},
		}

		userGroupIDs := make([]string, 0)
		menuItemUserGroupIDs(menuItems, 3, &userGroupIDs)
		fmt.Println("LMSLMS %+v", userGroupIDs)*/

	/*	//construct version store
		versionStore := Constructor()
		in := bufio.NewReader(os.Stdin)
		for {
			s, err := in.ReadString('\n')
			if err != nil {
				// io.EOF is expected, anything else
				// should be handled/reported
				if err != io.EOF {
					log.Fatal(err)
				}
				break
			}
			// Do something with the line of text
			// in string variable s.
			callCmd(&versionStore, s)
		}
		fmt.Println("LMSLMS", versionStore.Get("key2"))
		fmt.Println("LMSLMS", versionStore)*/
}
